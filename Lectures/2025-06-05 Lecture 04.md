by using a square function, why do we use one and what is the difference to a RMSE.
i need this to understand the cost function, so explain this in the context as well.
outliers have a bigger impact on the error. we do not want to have scattered predictions. the error is 0 but if the errors are very big appart, we want smooth predictions.
explain this by having a graph
we want to minimize the outliers

![[Pasted image 20250605103842.png]]

we have a tangent plain (green) to our current position / the current error (red dot) that shows us where to go downhill. This then tells us what weihgts an biases to adjust. I do not understand what that means? How do i know what weights and biases we can adjust here then? we are doing stocastical gradient descent and not just gradient descent, but whey is this better? 

in the beginning we have a high learning rate with exploration, we jump araount the space between down spots and plateaus and when we find something rather decent we try to find the mimimum (we hope that the minimum ist close to other minimums).

why does taking the squared error instead of the mean error have a bigger impact on the outcome than choosing the "right" optimizer?

### representation learning

we still want to have cleaned data but we do not do any preprocessing, we just remoce false data and noise but we do not need to give any hints to the algorithm that we want extracted.

since models are flexible a representative leanring approach is ... ???


## data preparation

### types of activation functions

sigmoid, tanh, ReL, Softmax

![[Pasted image 20250605111522.png]]
https://www.linkedin.com/pulse/types-activation-functions-sigmoid-tanh-relu-softmax-part-dave-uipuc/

## sliding window

having a step size of one is a bad idea for deployment but for training it is good beacse for the model the input now looks compleatly differnelty and this makes it more robust in the training. Explain why this is the case please.
we are kinda making more input data.

### adding random noise to the data

"true" because the random functions in every computer are not true random.
we do not want jittery noice we want a smooth curve because everyhting intthe world usually is a gradient from one to another. movement is not a direkt jump but a movement.
Perlin noice help with that as it does add the gradient in the data we want to argument, and not have random jumps.